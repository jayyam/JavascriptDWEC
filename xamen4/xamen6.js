/**Pide al usuario que te diga el nombre de los compañeros de su clase. A partir de esos nombres
 * crear una variable de tipo Map que muestre por pantalla cada nombre y las veces que se ha repetido.

 * */
function claseComp() {
    let user;
    let myMap = new Map();
    user = prompt("Inserta nombre de compañeros de clase:", "");
    if (user != "" && user != null) {
        myMap.set(a, 1);
        myMap.set(b, 2);
        myMap.set(c, 3);
        myMap.set(d, 4);
    }
}


