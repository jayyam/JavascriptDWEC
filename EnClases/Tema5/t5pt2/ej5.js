/**
 * Muestra por consola un evento cuando
 * Se haya cargado la página por completo (DOMContentLoaded)
 * Se haya cargado la página por completo y resto (hojas de estilos, y librerás js). (onload o load).
 * */

window.addEventListener('DOMContentLoaded', (event) =>
{
    console.log('DOM cargado');
});


